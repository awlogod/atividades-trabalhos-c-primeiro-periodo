#include <stdio.h>
#include <stdlib.h>

 int main() {

    int contador = 0, numero;

   /* for(contador = 5; contador <=  15; contador++){
        printf("o valor de C é: %d\n", contador);
    }
    printf("Fim do programa!\n");
    return 0; */

   /* while(contador <= 15){
        printf("o valor de C é: %d\n", contador);
        contador++;
    }
    printf("Fim do programa!\n");
    return 0; */

    /* O do while verifica a informacao pelo menos uma vez, mesmo que ela seje falsa*/

  /*  do {
    printf("Digite um numero:\n  ");
    scanf("%d", &numero);
    }while(numero == 20);

    printf("Número inválido: %d\n", numero);
    return 0; */

  /*  for(contador = 0; contador <= 20; contador++){
        printf("Numeros: %d \n", contador);
    }
    return 0; */
} 

